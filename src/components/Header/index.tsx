import NavBar from '../NavBar/index';
import TopBar from '../TopBar/index';

const Header = () => {
    return (
        <>
            <TopBar />
            <NavBar />
        </>
    );
};

export default Header;
